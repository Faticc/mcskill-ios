// The messages and clients of this module are generated at build time from the .proto files here
// (SwiftProtobufPlugin, GRPCSwiftPlugin). SwiftPM skips a target without a single Swift source,
// so this file keeps it one.
enum McSkillProtoModule {}
